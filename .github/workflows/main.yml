name: test-workflow

on:
  push: 
    branches:
      - main
jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v5.0.0
        
      - name: test action
        run: ls -lah
